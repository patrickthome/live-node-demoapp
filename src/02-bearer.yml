name: Bearer

 

on:

  push:

    branches:

      - main

  workflow_dispatch: 

 

permissions:

  contents: read

 

jobs:

  rule_check:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v7

      - name: Bearer

        uses: bearer/bearer-action@v2
